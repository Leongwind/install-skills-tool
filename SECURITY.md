# Security policy

Skill Installer 只在本机读取、校验和写入 Skill 目录，不上传遥测，也不会执行 Skill 自带的脚本。来源下载、ZIP 解压、路径规范化和原子写入都在 Rust 侧完成。

## 已知防护

- 拒绝绝对路径、路径穿越、控制字符文件名和软链接逃逸。
- ZIP 有压缩大小、展开大小和文件数限制。
- 下载先进入临时缓存，校验通过后才进入目标目录。
- 覆盖、卸载和恢复前创建备份；stale 计划会在重新校验失败时零写入返回。
- 诊断输出会将用户主目录折叠为 `~`，不包含 Skill 文件内容。

## 报告问题

请在 GitHub issue 中使用安全模板，提供版本、复现步骤和脱敏日志。不要上传 Skill 内容、访问令牌、私有仓库 URL、完整用户路径或备份文件。如果问题可能暴露本地数据，请先在 issue 中说明“需要私下沟通”，由维护者提供后续渠道。

在修复发布前请不要公开可利用的路径穿越、任意文件写入或凭据泄露细节。
